<h1>Rooms#show</h1>
<h1><%= @room.name %></h1>

<div id="messages">
  <%= render @messages %>
</div>

<%= form_with model: [@room, @message], data: { turbo: true } do |form| %>
  <p>
    <%= form.text_field :content %>
    <%= form.submit "Send" %>
  </p>
<% end %>
